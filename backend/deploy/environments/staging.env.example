CHETIWA_ENV=staging
PORT=8080
RADAR_ENABLED=true
RADAR_FREE_SESSIONS=20
RADAR_PREMIUM_SESSIONS=200
MONTHLY_BUDGET_CENTS=10000
RADAR_TILE_COST_CENTS=0
GLOBAL_KILL_SWITCH=false
# Staging uses the protected public tunnel while the provider is in beta.
RADAR_METADATA_URL=https://radar.ezplatforms.com/public/weather-maps.json
RADAR_TILE_URL_TEMPLATE=https://radar.ezplatforms.com{frame}/256/{z}/{x}/{y}/10/1_1.png
# SHARED_COUNTER_URL=https://shared-counter-staging.example/increment
# PUBLIC_BASE_URL=https://api-staging.chetiwa.example
# INTERNAL_METRICS_TOKEN=inject-from-secret-manager
GOOGLE_CLOUD_PROJECT=replace-with-chetiwa-staging-project-id
# Injecter les valeurs secrètes au runtime, jamais dans ce fichier :
# OPEN_METEO_API_KEY (Secret Manager)
# ARCGIS_API_KEY (Secret Manager, si ArcGIS est retenu)
# LibreWXR est le fournisseur bêta par défaut. Conserver l'attribution visible
# dans l'app et surveiller les erreurs/fraîcheur avant toute montée en charge.
